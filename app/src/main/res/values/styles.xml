<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:statusBarColor" type="color">?android:colorBackground</item>
        <item name="android:fontFamily">@font/mulish</item>
    </style>

    <style name="MenuButton" parent="Widget.AppCompat.ActionButton">
        <item name="android:fontFamily">@font/mulish</item>
    </style>
    <style name="InputButton" parent="Widget.AppCompat.Button.Borderless">
        <item name="android:textColor">@color/colorPrimaryFaded</item>
        <item name="android:textSize">24sp</item>
    </style>
</resources>